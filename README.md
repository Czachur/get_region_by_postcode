get_region_by_postcode
======================

Returns a region for a post code based on UK post code conventions.

E.g. 1 character followed by an integer (M33 6NZ) or two characters followed by an integer (SW1A 2AA).
